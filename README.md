# 🚗 Fixt. Nord — Intégration CarPlay & Auto Tech

Bienvenue sur le code source de **Fixt. Nord**, une solution web complète pour la gestion d'un atelier d'installation multimédia automobile. Ce projet inclut un site vitrine, un système de réservation en ligne et un panneau d'administration sécurisé.

![Statut](https://img.shields.io/badge/Statut-En_Production-success)
![Stack](https://img.shields.io/badge/Stack-HTML%20%7C%20Tailwind%20%7C%20Supabase-blue)

## ✨ Fonctionnalités

### 🖥️ Côté Client (Front-Office)

- **Landing Page Moderne** : Design "Glassmorphism" avec animations fluides et Tailwind CSS.
- **Réservation Intelligente** :
  - Sélection de services dynamiques (prix, durée).
  - Calendrier de disponibilités en temps réel (seuls les créneaux libres et ouverts apparaissent).
  - Formulaire de contact et validation.
- **Responsive** : Optimisé pour mobile, tablette et desktop.

### 🛡️ Côté Admin (Back-Office)

- **Authentification Sécurisée** : Protection des pages via Supabase Auth.
- **Dashboard** : Statistiques (CA, nombre de RDV) et activité récente.
- **Gestion du Planning** :
  - Vue calendrier (FullCalendar) pour ouvrir/fermer des créneaux.
  - Visualisation des RDV avec codes couleurs par statut.
- **Gestion des Réservations** : Confirmer, Terminer ou Annuler un RDV en un clic.
- **Gestion des Services** : Ajouter ou supprimer des prestations (prix, durée) directement depuis l'interface.

## 🛠️ Stack Technique

- **Frontend** : HTML5, CSS3, JavaScript (Vanilla ES6+).
- **Styling** : [Tailwind CSS](https://tailwindcss.com/) (via CDN).
- **Icônes** : FontAwesome 6.
- **Base de données & Auth** : [Supabase](https://supabase.com/) (PostgreSQL).
- **Calendrier** : FullCalendar.js.

## 🚀 Installation & Configuration

Ce projet est "Serverless" et statique. Il peut être hébergé n'importe où (Vercel, Netlify, OVH Cloud Web, GitHub Pages).

### 1. Cloner le projet

```bash
git clone [https://github.com/votre-pseudo/fixt-nord.git](https://github.com/votre-pseudo/fixt-nord.git)
cd fixt-nord
```

### 2\. Configuration Supabase

Le projet nécessite une instance Supabase. Créez un projet sur [supabase.com](https://supabase.com) et exécutez les requêtes SQL suivantes dans l'éditeur SQL pour créer les tables nécessaires :

```sql
-- Table des Services
create table services (
  id bigint generated by default as identity primary key,
  name text not null,
  price numeric not null,
  duration integer not null -- en minutes
);

-- Table des Créneaux (Disponibilités)
create table slots (
  id bigint generated by default as identity primary key,
  start_time timestamptz not null,
  end_time timestamptz not null
);

-- Table des Réservations
create table bookings (
  id bigint generated by default as identity primary key,
  created_at timestamptz default now(),
  customer_name text not null,
  email text,
  phone text,
  car_model text,
  service_name text,
  price numeric,
  start_time timestamptz not null,
  end_time timestamptz not null,
  status text default 'pending' -- pending, confirmed, finished
);
```

### 3\. Lier le projet

Ouvrez le fichier `script.js` et remplacez les constantes en haut du fichier par les vôtres :

```javascript
const SUPABASE_URL = "VOTRE_URL_SUPABASE";
const SUPABASE_KEY = "VOTRE_CLE_ANON_PUBLIC";
```

### 4\. Créer un Admin

Dans l'interface Supabase \> Authentication, créez un utilisateur avec votre email et mot de passe. C'est ce compte qui servira à se connecter sur `/login.html`.

## 📂 Structure du projet

```
/
├── index.html      # Accueil & Présentation
├── about.html      # Page À Propos
├── booking.html    # Tunnel de réservation
├── login.html      # Page de connexion Admin
├── admin.html      # Tableau de bord Admin (Protégé)
├── style.css       # Styles globaux & thèmes
├── script.js       # Toute la logique JS (Client + Admin)
└── img/            # Images & Logos
```

## 📄 Licence

**Copyright © 2024 Fixt. Nord. Tous droits réservés.**

Ce projet est la propriété exclusive de Fixt. Nord. Toute reproduction, distribution, modification ou utilisation commerciale de ce code source, des images ou du design sans autorisation écrite préalable est strictement interdite.
